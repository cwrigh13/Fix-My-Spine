<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Admin Panel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .rating-stars {
            display: inline-flex;
            align-items: center;
        }
        .star {
            width: 16px;
            height: 16px;
            color: #ffc107;
            margin-right: 2px;
        }
        .star-empty {
            width: 16px;
            height: 16px;
            color: #e9ecef;
            margin-right: 2px;
        }
        .badge-google {
            background-color: #4285f4;
            color: white;
        }
        .badge-patient {
            background-color: #28a745;
            color: white;
        }
        .stats-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
        }
        .api-status {
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .api-status.configured {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .api-status.not-configured {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
    </style>
</head>
<body class="bg-light">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/admin/dashboard">
                <i class="fas fa-cog me-2"></i>Admin Panel
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/admin/dashboard">Dashboard</a>
                <a class="nav-link" href="/admin/listings">Listings</a>
                <a class="nav-link active" href="/admin/google-ratings">Google Ratings</a>
                <a class="nav-link" href="/admin/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <!-- Header -->
        <div class="row">
            <div class="col-12">
                <h1 class="mb-4">
                    <i class="fab fa-google me-2"></i>Google Maps Ratings Management
                </h1>
            </div>
        </div>

        <!-- API Status -->
        <% if (apiKeyConfigured) { %>
            <div class="api-status configured">
                <i class="fas fa-check-circle me-2"></i>
                <strong>Google Maps API Key Configured</strong> - Ready to fetch ratings
            </div>
        <% } else { %>
            <div class="api-status not-configured">
                <i class="fas fa-exclamation-triangle me-2"></i>
                <strong>Google Maps API Key Not Configured</strong> - Please add GOOGLE_MAPS_API_KEY to your .env file
            </div>
        <% } %>

        <!-- Statistics -->
        <div class="row mb-4">
            <div class="col-md-3">
                <div class="stats-card text-center">
                    <h3><%= stats.total %></h3>
                    <p class="mb-0">Total Businesses</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card text-center">
                    <h3><%= stats.withGoogleRatings %></h3>
                    <p class="mb-0">With Google Ratings</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card text-center">
                    <h3><%= stats.withoutGoogleRatings %></h3>
                    <p class="mb-0">Without Google Ratings</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card text-center">
                    <h3><%= stats.averageGoogleRating.toFixed(1) %></h3>
                    <p class="mb-0">Avg Google Rating</p>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Bulk Actions</h5>
                        <div class="row">
                            <div class="col-md-4">
                                <form method="POST" action="/admin/google-ratings/update-all" class="d-inline">
                                    <div class="input-group">
                                        <input type="number" name="limit" class="form-control" placeholder="Limit (optional)" min="1" max="100">
                                        <button type="submit" class="btn btn-primary" <%= !apiKeyConfigured ? 'disabled' : '' %>>
                                            <i class="fas fa-sync me-2"></i>Update All Ratings
                                        </button>
                                    </div>
                                </form>
                            </div>
                            <div class="col-md-8">
                                <small class="text-muted">
                                    Updates Google Maps ratings for all businesses. Use limit to process only a subset for testing.
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Messages -->
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-circle me-2"></i><%= error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>

        <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle me-2"></i><%= success %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>

        <!-- Businesses Table -->
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-list me-2"></i>Businesses & Google Ratings
                </h5>
            </div>
            <div class="card-body">
                <% if (businesses && businesses.length > 0) { %>
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>Business Name</th>
                                    <th>Location</th>
                                    <th>Google Rating</th>
                                    <th>Patient Rating</th>
                                    <th>Last Updated</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% businesses.forEach(function(business) { %>
                                    <tr>
                                        <td>
                                            <div class="fw-bold"><%= business.business_name %></div>
                                            <% if (business.address) { %>
                                                <small class="text-muted"><%= business.address %></small>
                                            <% } %>
                                        </td>
                                        <td>
                                            <%= business.suburb %>, <%= business.state %> <%= business.postcode %>
                                        </td>
                                        <td>
                                            <% if (business.google_rating) { %>
                                                <div class="rating-stars mb-1">
                                                    <% for (let i = 1; i <= 5; i++) { %>
                                                        <% if (i <= Math.round(business.google_rating)) { %>
                                                            <i class="fas fa-star star"></i>
                                                        <% } else { %>
                                                            <i class="far fa-star star-empty"></i>
                                                        <% } %>
                                                    <% } %>
                                                </div>
                                                <div>
                                                    <span class="badge badge-google">
                                                        <%= business.google_rating.toFixed(1) %> (<%= business.google_review_count %>)
                                                    </span>
                                                </div>
                                            <% } else { %>
                                                <span class="text-muted">No Google rating</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (business.internal_avg_rating) { %>
                                                <div class="rating-stars mb-1">
                                                    <% for (let i = 1; i <= 5; i++) { %>
                                                        <% if (i <= Math.round(business.internal_avg_rating)) { %>
                                                            <i class="fas fa-star star"></i>
                                                        <% } else { %>
                                                            <i class="far fa-star star-empty"></i>
                                                        <% } %>
                                                    <% } %>
                                                </div>
                                                <div>
                                                    <span class="badge badge-patient">
                                                        <%= business.internal_avg_rating.toFixed(1) %> (<%= business.internal_review_count %>)
                                                    </span>
                                                </div>
                                            <% } else { %>
                                                <span class="text-muted">No patient reviews</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (business.google_last_updated) { %>
                                                <%= new Date(business.google_last_updated).toLocaleDateString() %>
                                                <br><small class="text-muted">
                                                    <%= new Date(business.google_last_updated).toLocaleTimeString() %>
                                                </small>
                                            <% } else { %>
                                                <span class="text-muted">Never</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <div class="btn-group btn-group-sm" role="group">
                                                <form method="POST" action="/admin/google-ratings/update-business" class="d-inline">
                                                    <input type="hidden" name="businessId" value="<%= business.id %>">
                                                    <button type="submit" class="btn btn-outline-primary btn-sm" 
                                                            <%= !apiKeyConfigured ? 'disabled' : '' %> 
                                                            title="Update Google rating for this business">
                                                        <i class="fas fa-sync"></i>
                                                    </button>
                                                </form>
                                                <form method="POST" action="/admin/google-ratings/clear-cache" class="d-inline">
                                                    <input type="hidden" name="businessId" value="<%= business.id %>">
                                                    <button type="submit" class="btn btn-outline-warning btn-sm" 
                                                            title="Clear Google rating cache">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                <% } else { %>
                    <div class="text-center py-4">
                        <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                        <p class="text-muted">No businesses found.</p>
                    </div>
                <% } %>
            </div>
        </div>

        <!-- Help Section -->
        <div class="card mt-4">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-question-circle me-2"></i>Help & Documentation
                </h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <h6>Getting Started</h6>
                        <ol>
                            <li>Get a Google Maps API key from <a href="https://console.cloud.google.com/" target="_blank">Google Cloud Console</a></li>
                            <li>Enable the Places API for your project</li>
                            <li>Add <code>GOOGLE_MAPS_API_KEY=your_key_here</code> to your .env file</li>
                            <li>Click "Update All Ratings" to fetch ratings for all businesses</li>
                        </ol>
                    </div>
                    <div class="col-md-6">
                        <h6>Features</h6>
                        <ul>
                            <li><strong>Automatic Matching:</strong> Finds businesses on Google Maps using name and address</li>
                            <li><strong>Rate Limiting:</strong> Respects Google API limits with delays</li>
                            <li><strong>Caching:</strong> Updates ratings weekly to minimise API calls</li>
                            <li><strong>Fallback:</strong> Shows patient reviews when Google ratings unavailable</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
